# Install torch BEFORE this file:
#   CUDA (RunPod/cloud):  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#   ROCm (local):         pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm7.2
# Then: pip install -r requirements.txt
#
# Note: train_unsloth.py requires unsloth[rocm] for local ROCm — install separately if needed
unsloth[cu124-torch260] @ git+https://github.com/unslothai/unsloth.git
unsloth_zoo
torchao<0.10.0          # torchao 0.10+ requires torch>=2.11, incompatible with cu124/torch 2.6
transformers>=4.51.0,<4.52.0  # pin to avoid pulling in torchao 0.10+ transitively
datasets>=3.0.0
trl>=0.15.0
peft>=0.14.0
accelerate>=1.3.0
sentencepiece
protobuf
einops
scipy
ninja
packaging
wheel
