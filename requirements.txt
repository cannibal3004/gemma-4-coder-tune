# CUDA requirements (RunPod/cloud GPU)
# Install torch BEFORE this file:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
# Then: uv pip install -r requirements.txt
unsloth @ git+https://github.com/unslothai/unsloth.git@2026.4.4
unsloth_zoo==2026.4.3
torchvision==0.21.0+cu126   # must match torch version installed by setup script
torchao==0.9.0              # 0.10+ requires torch>=2.11
transformers>=4.52.0,<5.0.0
datasets>=3.0.0
trl>=0.15.0
peft>=0.14.0
accelerate>=1.3.0
sentencepiece
protobuf
einops
scipy
ninja
packaging
wheel
